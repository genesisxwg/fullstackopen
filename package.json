{
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "4.1",
    "mongoose": "^9.9.5",
    "morgan": "^1.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "scripts": {
    "build:ui": "rm -rf dist && cd part2/phonebook && npm run build && cp -r dist ../..",
    "deploy:full": "npm run build:ui && git add . && git commit -m 'build for production' && git push",
    "dev": "nodemon index.js",
    "start": "node index.js"
  }
}
